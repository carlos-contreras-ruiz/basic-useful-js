//este comportamiento declara la variable al inicio del archico
//solo la declararacion var variableGlobal
var variableGlobal = "variable";

let variable = "variable con ambito de scope";

const constante = "este valor es constante se asigna en ejecucion";

console.log(variableGlobal);
